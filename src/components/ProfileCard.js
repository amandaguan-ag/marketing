const ProfileCard = () => {
  return (
    <section
      className="bg-white px-4 py-6 rounded-lg w-[340px] flex flex-col shadow-md mx-auto mt-[200px]"
      role="complementary"
    >
      {/* Add your content here */}
    </section>
  );
};

ProfileCard.propTypes = {
  // Define your prop types if needed
};

export default ProfileCard;